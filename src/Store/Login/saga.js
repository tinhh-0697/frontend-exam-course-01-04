import { takeLatest, put, call } from 'redux-saga/effects';
import * as ServiceAPI from '../Api/ServiceAPI';
// import * as LoginAction from '../../Store/Login/action';
import * as types from './type';

export function* Login() {
  // yield takeLatest( types., getProductRes);
  // yield takeLatest(API.api_3, getProductByIDRes);
  // yield takeLatest(API.api_5, getProductFromToRes);
}

export function* LoginFireBaseRes() {}

export function* CheckLoginFirebase() {}
